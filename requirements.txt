# Core data processing
pandas==1.5.3          # Final 1.x release, stable and fast, widely used
numpy==1.23.5          # Compatible with both pandas 1.5 and newer Python versions

# Excel I/O (optional, if handling Excel files)
openpyxl==3.1.2

# Date parsing utilities
python-dateutil==2.8.2

# CLI tools (if you're building a command-line tool)
click==8.1.7

# Progress bar (optional for tracking processing)
tqdm==4.66.1

# Optional: validation or configuration tools (if needed)
# pydantic==1.10.12     # Commented: add if schema validation is needed
# configparser==5.3.0   # Standard lib in Python, but can be pinned if you're using it externally

# Optional: testing
# pytest==7.4.2
